# WIP

# TornDiscordBot
A Simple TORN city API Discord Bot with a easy usage, Legal in torn city's automation rules. 
Used to call to torn api from discord, for monitoring

# How to use
- copy a file named `.env`, from `.env.example`
- configure `.env` by setting the values.
- run `npm install`
- run `node index.js`

# Commands
- `!ping` displays if bot is alive
- `!info` displays bot's information

<sub>whats torn city? this : [https://torn.com/](https://torn.com/4045988)</sub>
